const fs=require('fs'); const key=fs.readFileSync('.env.local', 'utf8').split('=')[1].trim().replace(/"/g, ''); const {GoogleGenerativeAI} = require('@google/generative-ai'); const ai = new GoogleGenerativeAI(key); async function test() { const models = ['gemini-1.5-flash', 'gemini-1.5-flash-latest', 'gemini-pro']; for (let m of models) { try { await ai.getGenerativeModel({model: m}).generateContent('hi'); console.log(m, 'SUCCESS'); } catch(e) { console.log(m, 'FAILED', e.message); } } } test();
